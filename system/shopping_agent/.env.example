# Every active LLM role uses this one local Ollama endpoint and model.
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b
OLLAMA_TIMEOUT_SECONDS=30

# Leave false in production. Build the catalogue cache in colab/bge_pipeline.ipynb.
ALLOW_CATALOG_EMBEDDING=false
