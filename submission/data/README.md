# Competition data mount

Do not commit the organizer catalogue or evaluation labels into this bundle.
For a standalone run, place `catalog.jsonl` here. When running from the main
repository, pass the official catalogue path to the evaluator launcher or set
`TECHJAM_CATALOG_PATH`.
